[develop]
script_dir=$base/lib/hsm_generator
[install]
install_scripts=$base/lib/hsm_generator

[tool:pytest]
markers =
    linter: the L0 tier - the coding style of the package
    unit: the L1 tier - the code generator, with no robot and no simulator
timeout = 60
